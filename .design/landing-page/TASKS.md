# Build Tasks: Road Devil® Landing Page

Generated from: [.design/landing-page/DESIGN_BRIEF.md](./DESIGN_BRIEF.md)
Reads: [.design/landing-page/INFORMATION_ARCHITECTURE.md](./INFORMATION_ARCHITECTURE.md), [.design/landing-page/TOKENS_LOCATION.md](./TOKENS_LOCATION.md)
Date: 2026-05-20

## Progress

- **2026-05-20** — `/design-flow` initial pass completed Tasks 1–6 (Foundation + the five flagged-item resolutions). TypeScript compiles cleanly. Stopped before the per-section polish passes because each one is a subjective craft call that benefits from designer review of the resolution work first. Resume with `/design-flow` or pick a specific section task to continue.

> Philosophy anchor: **Engineering-grade editorial**. Every task respects the existing token system (paper/ink/mute/rule/rd + Archivo + JetBrains Mono) and preserves the defensive/IP-heavy copy verbatim — polish lives in typography, spacing, motion, and hierarchy, never in re-wording.

The full one-pager already exists. These tasks are a **refinement and resolution pass** — closing the open questions from the brief/IA and tightening craft section by section.

## Foundation
- [x] **Resolve flagged-item decisions** ([Hero.tsx](../../components/Hero.tsx), [Nav.tsx](../../components/Nav.tsx), [Stack.tsx](../../components/Stack.tsx), [Technology.tsx](../../components/Technology.tsx)): Make one explicit decision on each of the five IA open questions — mobile sensor callouts, brochure CTA, mobile drawer CTA, Stack outcomes row, reduced-motion 3D tilt — and record the chosen direction in a comment block at the top of `TASKS.md` (or in a brief follow-up note). Without this, downstream tasks have ambiguous targets. _Reuses: brief + IA documents._
  - Decisions recorded: mobile sensor callouts → compact stacked legend below truck on `<md`; brochure CTA → re-label "See hardware" with right-arrow (no PDF asset exists); mobile drawer → add primary CTA inside drawer; Stack outcomes → keep both (distinct framing); reduced-motion → `useReducedMotion()` JS guards on Hero, Technology, Contact.

## Core UI — flagged-item resolutions (risk first)

- [x] **Mobile sensor callouts** ([Hero.tsx:233-249](../../components/Hero.tsx#L233-L249)): Replace the `hidden md:block` hide with a compact mobile variant — a stacked legend below the truck image listing the four sensors (RD-1418 · Forward AI, Telemetry · GNSS · 4G, rdDMS · Driver DMS, On-Device · Edge Inference), each row with a red dot + code chip + label, using the existing `.num-chip-red` and mono eyebrow styles. Desktop callouts unchanged. _Modifies: Hero._
- [x] **Reduced-motion audit for scroll-tilted dashboard** ([Technology.tsx:32-122](../../components/Technology.tsx#L32-L122)): Wrap the scroll-driven `rotateX` / `y` transforms in a `useReducedMotion()` guard (from `motion/react`) so users with `prefers-reduced-motion: reduce` get a flat, untilted dashboard image. Same check on Hero's mouse-parallax (`Hero.tsx:rotX/rotY/tx/ty`) and Contact's aurora drift. Decorative animation is already disabled in the reduced-motion CSS rule — this closes the JS-side gap. _Modifies: Hero, Technology, Contact._
- [x] **Hero brochure CTA decision** ([Hero.tsx:145-150](../../components/Hero.tsx#L145-L150)): The ghost CTA currently scrolls to `#hardware` but says "Download brochure". Either (a) replace destination with a real `/road-devil-brochure.pdf` asset and add `download` attribute, or (b) re-label to "See hardware" and keep the anchor. Default recommendation: (b) — until a PDF exists, the label must match the action. _Modifies: Hero._ — Done: relabelled "See hardware →", magnetic strength matched to primary CTA at 0.25.
- [x] **Mobile drawer primary CTA** ([Nav.tsx:110-137](../../components/Nav.tsx#L110-L137)): Add the "Request a strategic discussion" pill CTA to the bottom of the mobile hamburger drawer (below the six anchor links, separated by a `border-t border-rule pt-4`), so the conversion surface is reachable from the drawer on `sm` viewports where the nav-bar CTA is hidden. _Modifies: Nav._
- [x] **Stack outcomes row review** ([Stack.tsx:35-40, 245-275](../../components/Stack.tsx#L245-L275)): The four-outcome row inside the Stack section partially restates the Proof points. Decide in build: (a) keep both (the framing is different — Proof is "why this is different", outcomes is "what you get"), (b) merge into a single proof-points + outcomes block at the Proof position, or (c) tighten outcomes to two items and let it function as a "Stack summary" close. Default recommendation: (a) — keep both, but tighten outcome copy to one short line each (already done; verify in build). _Modifies: Stack (possibly no-op)._ — Verified: outcomes copy is already one short line each; framings are distinct from Proof. No-op.

## Core UI — per-section craft pass

Each of these is one independently buildable session per section. Order follows scroll order so a top-down review can verify the philosophy is holding before deeper sections are touched.

- [ ] **Hero polish pass** ([Hero.tsx](../../components/Hero.tsx)): Verify display H1 letter-spacing/leading at all breakpoints (`clamp(2.4rem, 4.4vw, 4.2rem)` with `leading-[1.0]`); confirm the italic-red "vehicle intelligence" line renders without baseline drift; check that the three hero stats (3 / 4 / UK) align baselines with `.tnum`; confirm `Magnetic` strengths on both CTAs are equal (currently 0.3 / 0.2 — recommend matching at 0.25 for parity); verify aurora opacity in dark mode is not over-saturated. _Modifies: Hero._
- [ ] **Proof section polish** ([Proof.tsx](../../components/Proof.tsx)): Verify the four cards have equal heights at `lg` even when body copy varies; confirm the hover-sweep radial gradient is bound to `--x`/`--y` CSS vars updated by `onMouseMove` (it currently has the gradient but no JS to update the position vars — either wire the mouse handler or remove the `var(--x)` reference); confirm icon glyph color transitions to `--rd` on hover. _Modifies: Proof._
- [ ] **Stack section polish** ([Stack.tsx](../../components/Stack.tsx)): Verify the 3D tile arrangement behaves on `md` and `sm` (currently the stack reads on desktop but may overflow on narrow viewports — confirm or constrain `aspect-[5/4]`); confirm keyboard focus moves through descriptor cards in correct order and triggers the same `active` index change as hover; verify L2 tile's dark gradient reads against the section's paper-2 background without losing edge definition. _Modifies: Stack._
- [ ] **Technology section polish** ([Technology.tsx](../../components/Technology.tsx)): Confirm the rdHub dashboard image (`/technology-dashboard.png`) has sufficient resolution at `1320px` width without softness; verify the two glass labels (rdHub · Live Console, 4 Modules · Unified) have legible contrast in dark mode; confirm the four module cards' `7rem` number watermarks (`01–04`) don't bleed beyond card edges on narrow grids. _Modifies: Technology._
- [ ] **Console section polish** ([Console.tsx](../../components/Console.tsx)): Verify the feature list (F.01–F.05) and stats row (6+ / <1s / 24/7 / EU) align on a shared baseline grid; confirm the mouse-tracked rotateX/Y spring (stiffness 70, damping 18) feels consistent with Hero's (stiffness 80, damping 18) — recommend matching for system coherence; ensure the multi-channel framing reads as a console screen, not a card. _Modifies: Console._
- [ ] **Hardware section polish** ([Hardware.tsx](../../components/Hardware.tsx)): Verify the four partner-mode statements use consistent terminal punctuation (currently each ends with `.`); confirm the halo `useTransform` scroll-driven opacity ramp (0 → 1 → 0.6) reads as intentional; check the reference-device image renders sharply in both themes; verify the section's CTA (if present) routes to `#contact`. _Modifies: Hardware._
- [ ] **Deployment section polish** ([Deployment.tsx](../../components/Deployment.tsx)): Verify the staircase 3D entrance (`rotateY: -8 → 0`, `x: -30 → 0`) plays once and doesn't replay on re-entry (`viewport={{ once: true }}` is set — confirm); confirm the four commercial models (C.01–C.04) sit visually subordinate to the three pathways (P.01–P.03), not as a competing grid; verify spacing parity between the two sub-grids. _Modifies: Deployment._
- [ ] **Strategic section polish** ([Strategic.tsx](../../components/Strategic.tsx)): Verify the five strengths fit a 3-column `lg` grid without an orphan (currently 5 items in a 3-col grid means 3 + 2 + gap — confirm visual intent or pad to 6); confirm the closing paragraph reads as a hand-off into the Contact section (no orphan eyebrow before Contact). _Modifies: Strategic._
- [ ] **Contact section polish** ([Contact.tsx](../../components/Contact.tsx)): Verify the dual aurora orbs don't compete with the form for attention (reduce orb opacity or blur if so); confirm the form fields have associated `<label>` elements (the `.field` class style hides default chrome — labels must still exist for screen readers); verify the submit state (`sent`) transitions cleanly and announces via `aria-live="polite"`; ensure the dark section's `.section-dark` token pinning is keeping the form text readable. _Modifies: Contact._
- [ ] **Footer polish** ([Footer.tsx](../../components/Footer.tsx)): Verify the top hairline gradient glow reads at the seam between Contact and Footer (both are `section-dark` — the seam should not look like a single block); confirm trademark statement and address columns sit on the same baseline grid. _Modifies: Footer._
- [ ] **Nav polish** ([Nav.tsx](../../components/Nav.tsx)): Verify the scroll-state transition (transparent → `bg-paper/70 backdrop-blur-xl`) feels at `12px` rather than triggering jitter on slow scrolls; confirm the six anchor links are in the same order as the scroll order (Stack → Technology → Console → Hardware → Deployment → Contact); verify the hamburger animation's three lines settle on exact x-axis when collapsing back to closed state. _Modifies: Nav._

## Interactions & States

- [ ] **Theme toggle parity** ([ui/ThemeToggle.tsx](../../components/ui/ThemeToggle.tsx)): Confirm the toggle visibly reflects current theme (icon swap or position shift), persists to `localStorage`, and the `html.theme-ready` transition class is applied after first paint so toggling animates while initial load does not flash. Covers: light → dark, dark → light, system-preference initial load, manual override persistence across reloads. _Modifies (if needed): ThemeToggle._
- [ ] **Scroll progress accuracy** ([ui/ScrollProgress.tsx](../../components/ui/ScrollProgress.tsx)): Verify the progress bar reaches 100% exactly at the bottom of the Footer (not earlier, not later); confirm it doesn't overflow horizontally and that its z-index sits above the Nav's blur layer. _Modifies (if needed): ScrollProgress._
- [ ] **Focus management sweep**: Confirm `:focus-visible` 2px `--rd` outline appears on every interactive element across all sections; verify the focus order through Nav → Hero CTAs → each section's interactive cards/buttons → Contact form fields → submit is logical and matches visual order. Covers: tab order, focus visibility light + dark, form field focus border-bottom shift. _Touches: all interactive components._

## Responsive & Polish

- [ ] **Breakpoint verification** at `375` / `768` / `1024` / `1280` / `1536`: Each section reads at every breakpoint; no horizontal scroll; sticky nav doesn't cover anchor targets when jumping (verify `scroll-padding-top` on `<html>` or anchored sections account for the 68px nav). Use the design-review skill to drive screenshots. _Touches: all sections._
- [ ] **Light + dark contrast audit**: For every body text + background pairing across all sections (paper/ink, paper-2/ink, paper-3/ink, section-dark/paper, card-dark/paper), verify ≥ 4.5:1 (body) and ≥ 3:1 (large text). Particular attention to mute text on paper-2 and on section-dark backgrounds. Document any failures and adjust token values (last resort) or class usage (preferred). _Touches: all sections._
- [ ] **Accessibility pass**: Verify (a) one `<h1>` per page (Hero), (b) descending `h2 → h3` hierarchy within sections (no `h2` after an `h3`), (c) every `<img>` has meaningful `alt` or `aria-hidden` if decorative, (d) all motion respects `prefers-reduced-motion`, (e) all anchor links use semantic `<a href="#…">`, (f) form has labels + error/success announcements. _Touches: all sections._

## Review

- [ ] **Design review**: Run [/design-review](../../.claude/skills/design-review) against the brief and IA. Captures screenshots at three breakpoints in both themes; produces `DESIGN_REVIEW.md` with must-fix / should-fix / nice-to-have categorisation against the *Engineering-grade editorial* philosophy. Run only after the polish passes are complete.
